/* ============================================================
   紫枫免费小说 · 认证上下文 (AuthContext)
   ============================================================ */

import { createContext } from 'react';

export const AuthContext = createContext();
