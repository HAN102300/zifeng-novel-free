/* ============================================================
   紫枫免费小说 · 主题上下文 (ThemeContext)
   ============================================================ */

import { createContext } from 'react';

export const ThemeContext = createContext();
