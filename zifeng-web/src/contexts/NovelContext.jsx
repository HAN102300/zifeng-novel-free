/* ============================================================
   紫枫免费小说 · 小说数据上下文 (NovelContext)
   ============================================================ */

import { createContext } from 'react';

export const NovelContext = createContext();
